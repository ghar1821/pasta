<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- WARNING: auto-generated file. -->
<project basedir="." default="build" name="assessment">
    <property environment="env"/>
    <property name="debuglevel" value="source,lines,vars"/>
    <property name="target" value="1.7"/>
    <property name="source" value="1.7"/>
	<property name="repeats" value="${repeats}"/>
	<path id="lib.jars">
		<fileset dir="${libdirectory}">
			<include name="**/*.jar"/>
		</fileset>
	</path>
	<path id="competition.classpath">
        <pathelement location="bin"/>
		<path refid="lib.jars"/>
    </path>
    <target name="init">
        <mkdir dir="bin"/>
        <copy includeemptydirs="false" todir="bin">
            <fileset dir="${compCodeDirectory}">
                <exclude name="**/*.java"/>
            </fileset>
        </copy>
    </target>
    <target name="clean">
        <delete dir="bin"/>
    </target>
    <target depends="init" name="build">
        <echo message="${ant.project.name}: ${ant.file}"/>
        <javac fork="true" includeantruntime="false" debug="true" debuglevel="${debuglevel}" destdir="bin" source="${source}" target="${target}">
            <src path="."/>
			<classpath refid="competition.classpath"/>
        </javac>
    </target>
	<target name="norepeat" if="repeats">
		<delete file="config.txt"/>
	</target>
	<target name="compete" >
		<antcall target="norepeat"/>
		<java classname="${mainClassname}" fork="true" timeout="${compTimeout}" dir="${basedir}" output="${basedir}/${outputFilename}" error="${basedir}/${errorFilename}">
			${arenaArgs}
			<classpath refid="competition.classpath"/>
		</java>
	</target>
	<target name="mark">
	</target>
</project>
