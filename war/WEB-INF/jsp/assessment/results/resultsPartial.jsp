<%@ taglib tagdir="/WEB-INF/tags" prefix="tag"%>
<tag:unitTestResult 
	closedAssessment="${closedAssessment}" 
	results="${results}" 
	detailsLink="${detailsLink}"
	separateGroup="${separateGroup}"
	summary="${summary}"
/>